#!/usr/bin/env bash

for f in csv/gamma/1/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/2/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/3/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/4/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/5/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done


for f in csv/gamma/6/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/7/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/8/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/9/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/10/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done


for f in csv/gamma/11/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/12/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/13/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/14/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/15/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done



for f in csv/gamma/16/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/17/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/18/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/19/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done

for f in csv/gamma/20/*.csv; do
  python3 adapt_distribution.py -pc "$f"
done